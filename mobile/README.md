# Mobile app for Rubaih Greeks

Not bundled in v1. Use the same pattern as Rubaih futures mobile later, pointed at:

- API base: `http://YOUR_VPS:8088`
- Token: `RUBAIH_GREEKS_API_TOKEN`
- Endpoints: `/api/dashboard`, `/api/settings`, `/api/kill`

Until then, curl/health checks are enough for day-1 dry-run.
